
# dir: ${hydra:runtime.output_dir}/exported   # ← clear, colocated with hydra logs
dir: exported_model   # relative to Hydra run dir — no timestamp needed
weights:        model_state.pth
input_scaler:   input_scaler.joblib
target_scaler:  target_scaler.joblib
cat_encoder:    cat_encoder.joblib
metadata:       metadata.json