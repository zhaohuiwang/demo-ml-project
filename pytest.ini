
# project-root/pytest.ini

[pytest]
minversion = 7.0
addopts = -ra --strict-markers
testpaths = tests
python_files = test_*.py
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks integration/smoke tests